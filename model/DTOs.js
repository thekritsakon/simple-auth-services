const userDTO = {
    email: "",
    password: ""
};

const loginDTO = {
    token: "",
    message: "",
    success: false,
}

const responseDTO = {
    message: "",
    success: false,
}

module.exports = {
    userDTO: userDTO,
    responseDTO: responseDTO,
    loginDTO: loginDTO
}
